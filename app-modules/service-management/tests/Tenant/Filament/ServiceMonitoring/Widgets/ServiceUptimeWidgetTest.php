<?php

it('displays the correct uptime percentage', function () {
    // make factories

    // $stats = (new Widget())->getStats();

    // expect($stats[0]->getValue())->toBe(3)
    //     ->and($stats[1]->getValue())->toBe(2);
})->todo();

it('displays N/A if there is not data for that period', function () {

})->todo();