version: 2
updates:
    # Enable version updates for GitHub Actions
    - package-ecosystem: 'github-actions'
      directory: '/'
      schedule:
          interval: 'weekly'
      cooldown:
          default-days: 7
      groups:
          github-actions-group:
              patterns:
                  - '*' # Groups all GitHub Actions updates
              update-types:
                  - 'minor'
                  - 'patch' # Only groups minor and patch updates

    # Enable version updates for npm (root, portals, widgets)
    - package-ecosystem: 'npm'
      directories:
          - '/'
          - '/portals'
          - '/widgets'
      schedule:
          interval: 'weekly'
      cooldown:
          default-days: 7
      groups:
          npm-group:
              patterns:
                  - '*' # Groups all npm updates
              update-types:
                  - 'minor'
                  - 'patch' # Only groups minor and patch updates
      ignore:
          - dependency-name: '*'
            update-types:
                - 'version-update:semver-major' # Never auto-bump major versions
